# AGENTS.md

## What this repo is

Personal GitHub profile README (`README.md`) with ASCII art, tech stack badges, and contact links. Not an application — there is no build system, test runner, linter, formatter, typechecker, CI, or package manager config.

## Structure

- `README.md` — profile page displayed on github.com/mohaned122
- `LICENSE` — MIT
- `assest/listofimages.txt` — unused placeholder file (contains only `hello`)

## Key constraints

- **Do not add, remove, or rewrite** `README.md` — it is a personal profile, not project documentation. Only edit if the user explicitly asks.
- The `assest/` directory name is misspelled (not `assets`); preserve the existing name.
- There are no npm, Angular, or other framework artifacts in this subtree. The `angular/` parent directory is just an organizational folder; do not assume any Angular tooling is configured here.

## Commands

No commands to run. There is no test/lint/build/start workflow.
